# CHANGES

## feat:(add rocket form)

- Add `src/components/AddRocketDialog.vue` as form to add new rocket data on page list
- Split the store into `apiRockets` + `localRockets` behind one computed
- Add `NewRocket` type in `src/types/rocket.ts`
